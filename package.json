{
  "name": "rootcastle-browser-poker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx -p typescript tsc -p tsconfig.server.json && npx -p typescript tsc -p tsconfig.client.json",
    "start": "node dist/server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" }
}
